<ContentDialog
    x:Class="UWP_VendaEntrades.CDs.AfegirClientCD"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_VendaEntrades.CDs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Afegir Client"
    PrimaryButtonText="Acceptar"
    SecondaryButtonText="Cancelar"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <StackPanel>
        <StackPanel>
            <Border x:Name="brdNIF">
                <TextBox x:Name="tbxNIF" Header="*NIF:"/>
            </Border>
            <Border x:Name="brdNom">
                <TextBox x:Name="tbxNom" Header="*Nom:"/>
            </Border>
            <Border x:Name="brdCognom1">
                <TextBox x:Name="tbxCognom1" Header="*Cognom1:"/>
            </Border>
            <Border x:Name="brdCognom2">
                <TextBox x:Name="tbxCognom2" Header="Cognom2:"/>
            </Border>
        </StackPanel>
    </StackPanel>
</ContentDialog>
